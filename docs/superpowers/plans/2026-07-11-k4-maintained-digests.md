# K4: Maintained digests — admin-defined markdown, regenerated when sources change, in agent context from second one\n\n> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.\n\n**Goal:** An admin defines a digest (slug, title, standing instructions, source Collections). A scheduler pass fingerprints the source corpora's chunk content (reusing K3's `src.knowledge_packaging.corpus_fingerprint`) and — only when the fingerprint changed — runs one LLM job (concurrency 1, budget/timeout-capped) that regenerates the digest's `output_md`. Failures keep the previous markdown and mark the digest **visibly stale** (`status` + `status_reason`) — never silent, never half-written. The sync manifest lists digests inside the existing `knowledge_artifacts` section (`kind: \"digest\"` — the exact seam K3 left open), and `agnes pull` writes `.claude/rules/ka_<slug>.md` next to the corporate-memory `km_*.md` bundle, so the digest (staleness banner included) is in the agent's context at session start. Admin CRUD ships on all three surfaces (REST + `/admin` page + CLI + MCP, `_COHORT` ratchet); read/distribution access rides standard `resource_grants` via a new `ResourceType.KNOWLEDGE_DIGEST`.\n\n**Architecture:** New table `knowledge_digests` (BOTH backends + contract test + BOTH migration ladders — the ONLY schema change). New engine module `src/knowledge_digests.py` (fingerprint short-circuit + LLM generation via the existing `connectors.llm` extractor factory + `run_digest_pass()` in the `_run_materialized_pass` posture: per-row try/except, aggregated summary). New admin trigger `POST /api/admin/run-knowledge-digests` + scheduler job (mirrors `run-knowledge-packaging`). Manifest: digest entries appended to `_build_knowledge_artifacts_section` (`app/api/sync.py:1164`) with an `md5` change-token; content served by `GET /api/knowledge/digests/{digest_id}/content`; `cli/lib/pull.py` grows `_sync_knowledge_digests` writing/pruning `.claude/rules/ka_<slug>.md` (the `km_*.md` delivery pattern at `cli/lib/pull.py:989`). Slice K4 of `docs/superpowers/specs/2026-07-10-unified-knowledge-design.md` (#799).\n\n**Tech Stack:** Python/FastAPI, DuckDB + Postgres repo pair, Alembic, `connectors/llm` (`create_extractor_from_env_or_config`, `extract_json`), scheduler sidecar (`services/scheduler/__main__.py`), Typer CLI, FastMCP (`app/api/mcp_http.py`), Jinja2 `base_ds.html` design system.\n\n## Global Constraints\n\n- **Naming decision (spec drift — record in PR description):** the spec names the table `knowledge_artifacts`, but K3 already shipped a *manifest section* named `knowledge_artifacts` (`app/api/sync.py:1164`, `cli/lib/pull.py:847`, sync-state key, `PullResult.knowledge_*` fields). Reusing the name for a table would make \"knowledge_artifacts\" mean two different things in the same codebase. The table, repo, and ResourceType are therefore named **`knowledge_digests`** / **`knowledge_digests_repo`** / **`knowledge_digest`** (id prefix `kd_`); the manifest section name stays `knowledge_artifacts` and digests ride it as `kind: \"digest\"` entries — exactly the seam K3's module docstring promised (`src/knowledge_packaging.py:15-18`). Be consistent everywhere; the word \"artifact\" in K4 code refers only to the manifest seam.\n- **Migration discipline — RE-VERIFY NUMBERS AT IMPLEMENTATION TIME.** On `zs/k3-local-knowledge-packaging` today: `SCHEMA_VERSION = 88` (`src/db.py:50`), newest DuckDB step `_v87_to_v88` (`src/db.py:5522`) dispatched at BOTH sites (fresh-snapshot branch `src/db.py:~5890`, sequential-upgrade branch `src/db.py:~6119`), newest Alembic revision `0035_parent_file_id_v88` (`migrations/versions/0035_parent_file_id_v88.py`, `down_revision = \"0034_marketplace_ref_pin_v87\"`). K4 branches off `main` after PR #823 merges and main moves fast (K1 was renumbered mid-flight): before writing a line, run `grep -n \"SCHEMA_VERSION = \" src/db.py` and `ls migrations/versions/` and renumber v89/`0036_*` below accordingly. Alembic ↔ `src/db.py` ladders land together (CONTRIBUTING sync-map); `_SYSTEM_SCHEMA` carries the DDL for fresh installs and the ladder step is an idempotent `CREATE TABLE IF NOT EXISTS` (the `_v81_to_v82` Collections precedent, `src/db.py:5864`).\n- **Repo sync-map:** DuckDB repo + `_pg.py` sibling + `_REGISTRY` entry + factory + cross-engine contract test in THIS PR (template: `src/repositories/corpus_files.py`, `src/repositories/__init__.py:430-432`, `tests/db_pg/test_corpus_files_contract.py`).\n- **Triple-surface ratchet:** admin CRUD paths go in `_COHORT` of `tests/test_documentation_api_triple_surface.py` (CLI `agnes admin digest …` + MCP tools in `app/api/mcp_http.py` — the ratchet checks that module, NOT `cli/mcp/server.py`). The scheduler trigger and the pull-consumed content endpoint are `_EXEMPT` with honest reasons (the `run-knowledge-packaging` / artifact-download precedents). Every new route also needs a `tests/db_pg/test_endpoints_smoke.py` COVERED_ROUTES entry (or the file's covered-elsewhere list with an honest pointer) AND a `docs/api-reference.md` line (`tests/test_api_docs_coverage.py` gate). Nothing joins `tests/api_triple_surface_grandfathered.txt`.\n- **RBAC decision (flagged):** CRUD is `require_admin`. Distribution/read is a **new `ResourceType.KNOWLEDGE_DIGEST`** registered as a `ResourceTypeSpec` in `app/resource_types.py` (application-level config, NO DB migration — the module docstring says so explicitly, `app/resource_types.py:22-25`); grants are ordinary `resource_grants` rows managed on the existing `/admin/access` page. The alternative — deriving access from the source corpora's `COLLECTION` grants — was rejected: a digest is a *derived* document (partial-grant leaks; ALL-of-N semantics gets fragile as the admin edits `source_corpus_ids`), and the spec says \"standard `resource_grants` mechanism\". Manifest + content endpoint filter fail-closed: ungranted or unknown digest → **404** (no existence leak), admin short-circuit via `can_access` exactly like `_accessible_corpus_ids` (`app/api/collections.py:180-192`).\n- **Failure semantics (never silent, never half-written):** generation results are committed as ONE repo call (`set_generated`: `output_md` + `source_fingerprint` + `model` + `generated_at` + `status='fresh'` + reason cleared). Any failure path (LLM error, no API key, per-pass budget/deadline) calls only `mark_stale(reason)` — previous `output_md`, fingerprint, and `generated_at` survive untouched, so the next pass retries and the old content keeps shipping WITH a staleness banner that travels all the way into `ka_<slug>.md`.\n- **Budget/timeout knobs (spec drift — record in PR description):** the spec says knobs \"mirror the `ingest.llm.*` family\", but that family (2026-07-08 spec: `per_file_budget_usd`, `daily_budget_usd`, `timeout_seconds`) is not implemented anywhere yet and the `StructuredExtractor` protocol returns parsed JSON only — no usage/cost data (`connectors/llm/base.py:14-40`), so USD budgets would be fiction. K4 ships the honest equivalents under the same naming style: `digests.llm.max_tokens` (response cap, default 4000), `digests.llm.timeout_seconds` (per-**pass** wall-clock deadline, default 300, `0` disables), `digests.llm.max_generations_per_pass` (LLM-call cap per pass, default 3, `0` disables). Read via `app.instance_config.get_value` (`app/instance_config.py:157`). Concurrency is 1 by construction (sequential loop). Provider/API key/model come from the existing `ai:` block via `create_extractor_from_env_or_config` (`services/corporate_memory/collector.py:441-448` precedent).\n- **Scope guards:** no live source connectors; no digest versioning/history (one row, latest output only); no per-digest schedule (one pass cadence); slug immutable after create (it is a filename on every analyst laptop); digests with empty `output_md` (status `pending`, never generated) are not distributed.\n- Vendor-agnostic; CHANGELOG bullet; no AI attribution; full suite (`.venv/bin/pytest tests/ --tb=short -n auto -q`) before push.\n\n**Task dependency graph (parallelism):**\n\n```\nTask 1 (migration, both ladders)\n  └─ Task 2 (repo pair + factory + contract test)\n       ├─ Task 3 (ResourceType + admin CRUD REST)   ─┬─ Task 6 (manifest entries + content endpoint) [P]\n       │                                             ├─ Task 7 (CLI + MCP, _COHORT)                  [P]\n       │                                             └─ Task 8 (/admin UI page)                      [P]\n       └─ Task 4 (engine: fingerprint + LLM pass)  [P with 3]\n            └─ Task 5 (admin trigger + scheduler job)\nTask 9 (agnes pull → ka_<slug>.md)  — independent (stub-server tests, frozen manifest contract) [P with 1–8]\nTask 10 (docs, CHANGELOG, full suite) — last\n```\n\n---\n\n### Task 1: `knowledge_digests` table — v88→v89, BOTH ladders\n\n**Files:**\n- Modify: `src/db.py` (`SCHEMA_VERSION`, `_SYSTEM_SCHEMA` DDL, `_v88_to_v89`, both dispatch sites)\n- Create: `migrations/versions/0036_knowledge_digests_v89.py`\n- Test: extend `tests/test_db_schema_version.py`\n\n> ⚠ Re-verify `SCHEMA_VERSION`, the ladder tail, and the Alembic head on the actual base commit before starting (see Global Constraints). Everything below assumes 88/`0035_parent_file_id_v88`.\n\n**DDL (single source for both ladders):**\n\n```sql\n-- v89: K4 maintained digests (#799). Admin-defined digest documents the\n-- scheduler regenerates via LLM when their source corpora change.\n-- status: pending (never generated) | fresh | stale (sources changed but\n-- regeneration failed/deferred — output_md is the last good generation,\n-- status_reason says why). source_corpus_ids is a JSON array stored as text.\nCREATE TABLE IF NOT EXISTS knowledge_digests (\n    id                 VARCHAR PRIMARY KEY,\n    slug               VARCHAR NOT NULL UNIQUE,\n    title              VARCHAR NOT NULL,\n    instructions       TEXT NOT NULL,\n    source_corpus_ids  VARCHAR,\n    output_md          TEXT,\n    source_fingerprint VARCHAR,\n    generated_at       TIMESTAMP,\n    model              VARCHAR,\n    status             VARCHAR DEFAULT 'pending',\n    status_reason      VARCHAR,\n    created_by         VARCHAR,\n    created_at         TIMESTAMP DEFAULT current_timestamp,\n    updated_at         TIMESTAMP DEFAULT current_timestamp\n);\n```\n\n(Column set = spec list + `status_reason` — the spec's failure semantics demand \"status + reason\", so the reason needs a column; + the house `created_by/created_at/updated_at` bookkeeping. Note the addition in the PR description.)\n\n- [ ] **Step 1: Failing test** — append to `tests/test_db_schema_version.py` (follow the file's fresh-install idiom, e.g. `test_v39_adds_marketplace_plugins_is_system` at `tests/test_db_schema_version.py:275`):\n\n```python\ndef test_v89_knowledge_digests_table(tmp_path):\n    \"\"\"v89 (K4, #799): knowledge_digests exists on fresh installs, ladder is\n    idempotent, and schema_version lands at SCHEMA_VERSION.\"\"\"\n    import duckdb as _duckdb\n\n    conn = _duckdb.connect(str(tmp_path / \"system.duckdb\"))\n    _ensure_schema(conn)\n    cols = {r[1] for r in conn.execute(\"PRAGMA table_info('knowledge_digests')\").fetchall()}\n    assert {\"id\", \"slug\", \"title\", \"instructions\", \"source_corpus_ids\", \"output_md\",\n            \"source_fingerprint\", \"generated_at\", \"model\", \"status\", \"status_reason\"} <= cols\n    assert get_schema_version(conn) == SCHEMA_VERSION\n    # idempotency — re-running the step must not raise\n    from src.db import _v88_to_v89\n    _v88_to_v89(conn)\n    conn.close()\n```\n\nAlso extend the running comment ladder in `test_schema_version_is_62` (`tests/test_db_schema_version.py:17` — the assert is `SCHEMA_VERSION >= 80`, no bump needed; add the v88→v89 comment line for the archaeology trail).\n\n- [ ] **Step 2: Run** `.venv/bin/pytest tests/test_db_schema_version.py -q` → FAIL\n- [ ] **Step 3: Implement** in `src/db.py`:\n  - `SCHEMA_VERSION = 89` (`src/db.py:50`).\n  - Add the DDL block to `_SYSTEM_SCHEMA` right after the v82 Collections tables (`src/db.py:~1380`, after `corpus_chunks`).\n  - New step next to `_v87_to_v88` (`src/db.py:5522`):\n\n```python\ndef _v88_to_v89(conn: duckdb.DuckDBPyConnection) -> None:\n    \"\"\"v88→v89: ``knowledge_digests`` — maintained digests (K4, #799).\n\n    Idempotent CREATE TABLE IF NOT EXISTS; fresh installs already get the\n    table from ``_SYSTEM_SCHEMA`` (no-op here). Sequential upgrades from a\n    v88 instance create it now.\n    \"\"\"\n    conn.execute(\"\"\"<the DDL above>\"\"\")\n    conn.execute(\"UPDATE schema_version SET version = 89\")\n```\n\n  - Dispatch at BOTH sites: after `_v87_to_v88(conn)` in the fresh-snapshot branch (`src/db.py:~5890`, with the house one-comment-per-step style) and `if current < 89: _v88_to_v89(conn)` in the sequential-upgrade branch (`src/db.py:~6119`).\n  - `migrations/versions/0036_knowledge_digests_v89.py` (mirror `0035_parent_file_id_v88.py`): `revision = \"0036_knowledge_digests_v89\"`, `down_revision = \"0035_parent_file_id_v88\"`, `upgrade()` = `op.create_table(\"knowledge_digests\", sa.Column(\"id\", sa.String(), primary_key=True), sa.Column(\"slug\", sa.String(), nullable=False, unique=True), …, sa.Column(\"created_at\", sa.TIMESTAMP(), server_default=sa.text(\"now()\")), …)`, `downgrade()` = `op.drop_table(\"knowledge_digests\")`.\n- [ ] **Step 4: Run** `.venv/bin/pytest tests/test_db_schema_version.py -q` → PASS (PG ladder is exercised by Task 2's contract test via `alembic upgrade head`).\n- [ ] **Step 5: Commit** `feat(db): knowledge_digests table — v89 + alembic 0036 (K4)`\n\n---\n\n### Task 2: repository pair + factory + cross-engine contract test\n\n**Files:**\n- Create: `src/repositories/knowledge_digests.py`, `src/repositories/knowledge_digests_pg.py`\n- Modify: `src/repositories/__init__.py` (`__all__` + `_REGISTRY` + factory — 3 edits, copy the `\"corpus_files\"` entry style at `src/repositories/__init__.py:430-432,683`)\n- Test: `tests/db_pg/test_knowledge_digests_contract.py`\n\n**Interfaces (both backends identical — the contract):**\n\n```python\nclass KnowledgeDigestsRepository:            # DuckDB; Pg sibling takes the engine\n    def create(self, *, slug, title, instructions,\n               source_corpus_ids: list[str], created_by: str) -> str   # \"kd_\" + token_hex(8)\n    def get(self, digest_id) -> dict | None                            # source_corpus_ids decoded to list\n    def get_by_slug(self, slug) -> dict | None\n    def list(self) -> list[dict]                                       # ORDER BY created_at\n    def update(self, digest_id, *, title=None, instructions=None,\n               source_corpus_ids=None) -> None                         # bumps updated_at; slug is NOT updatable\n    def set_generated(self, digest_id, *, output_md, source_fingerprint, model) -> None\n        # ONE statement: output_md, source_fingerprint, model, generated_at=now,\n        # status='fresh', status_reason=NULL, updated_at=now  (never-half-written invariant)\n    def mark_stale(self, digest_id, *, reason: str) -> None\n        # ONE statement: status='stale', status_reason=reason, updated_at=now —\n        # output_md / source_fingerprint / generated_at untouched\n    def delete(self, digest_id) -> None\n```\n\n`source_corpus_ids` is JSON text in the DB, decoded on every read (the `processing_detail` idiom, `src/repositories/corpus_files.py:41-52`). Template the DuckDB class on `src/repositories/corpus_files.py`, the PG class on `src/repositories/corpus_files_pg.py`.\n\n- [ ] **Step 1: Failing tests** (`tests/db_pg/test_knowledge_digests_contract.py`) — copy the construction scaffolding of `tests/db_pg/test_corpus_files_contract.py` verbatim (`_make_duckdb_repo` with `_ensure_schema`; `_make_pg_repo` with `alembic upgrade head` + `AGNES_DB_URL` + `db_pg.dispose()`; `@pytest.fixture(params=[\"duckdb\", \"pg\"])`), then the contract:\n\n```python\ndef test_create_get_roundtrip_decodes_corpus_ids(repo):\n    did = repo.create(slug=\"arch\", title=\"Architecture overview\",\n                      instructions=\"Maintain an overview of our architecture.\",\n                      source_corpus_ids=[\"col_a\", \"col_b\"], created_by=\"u1\")\n    assert did.startswith(\"kd_\")\n    row = repo.get(did)\n    assert row[\"slug\"] == \"arch\" and row[\"status\"] == \"pending\"\n    assert row[\"source_corpus_ids\"] == [\"col_a\", \"col_b\"]       # decoded list, both backends\n    assert row[\"output_md\"] is None and row[\"generated_at\"] is None\n    assert repo.get_by_slug(\"arch\")[\"id\"] == did\n\ndef test_slug_unique(repo):\n    repo.create(slug=\"dup\", title=\"A\", instructions=\"i\", source_corpus_ids=[], created_by=\"u\")\n    with pytest.raises(Exception):\n        repo.create(slug=\"dup\", title=\"B\", instructions=\"i\", source_corpus_ids=[], created_by=\"u\")\n\ndef test_update_edits_fields_not_slug(repo): ...\n    # update(title=..., instructions=..., source_corpus_ids=[\"col_c\"]) → all three change,\n    # slug unchanged, updated_at bumped; update with all-None is a no-op that doesn't raise\n\ndef test_set_generated_is_atomic_fresh(repo): ...\n    # set_generated(output_md=\"# D\", source_fingerprint=\"fp1\", model=\"claude-…\")\n    # → status 'fresh', status_reason None, generated_at set, output_md/fingerprint stored\n\ndef test_mark_stale_keeps_previous_output(repo): ...\n    # after set_generated, mark_stale(reason=\"LLM timeout\") → status 'stale',\n    # status_reason set, output_md STILL \"# D\", source_fingerprint STILL \"fp1\",\n    # generated_at unchanged\n\ndef test_delete_and_list(repo): ...\n```\n\n- [ ] **Step 2: Run** `.venv/bin/pytest tests/db_pg/test_knowledge_digests_contract.py -q` → FAIL (module missing)\n- [ ] **Step 3: Implement** both repos + registry: `_REGISTRY[\"knowledge_digests\"] = {DUCKDB: (\"src.repositories.knowledge_digests\", \"KnowledgeDigestsRepository\"), PG: (\"src.repositories.knowledge_digests_pg\", \"KnowledgeDigestsPgRepository\")}`, factory `def knowledge_digests_repo() -> Any: return _build(\"knowledge_digests\")`, `__all__` entry.\n- [ ] **Step 4: Run** → PASS (both engines)\n- [ ] **Step 5: Commit** `feat(repos): knowledge_digests repository pair + contract test (K4)`\n\n---\n\n### Task 3: `ResourceType.KNOWLEDGE_DIGEST` + admin CRUD REST — *[parallel with Task 4 after Task 2]*\n\n**Files:**\n- Modify: `app/resource_types.py` (enum member + `_knowledge_digest_blocks` + `RESOURCE_TYPES` entry)\n- Create: `app/api/knowledge_digests.py` (router `prefix=\"/api/admin/knowledge-digests\"`)\n- Modify: `app/main.py` (import + `include_router`, next to the other routers)\n- Modify: `tests/test_documentation_api_triple_surface.py` (`_COHORT` — entries land here, surfaces in Task 7), `tests/db_pg/test_endpoints_smoke.py`, `docs/api-reference.md`\n- Test: `tests/test_api_knowledge_digests.py`\n\n**Interfaces:**\n- `GET /api/admin/knowledge-digests` → `{\"items\": [digest…]}` (list, `output_md` truncated to a 280-char preview + `output_chars` count — the full body ships via detail only)\n- `POST /api/admin/knowledge-digests` → 201 digest; body `{slug, title, instructions, source_corpus_ids}`; slug validated `^[a-z0-9][a-z0-9_-]{0,63}$` (must satisfy the client's `_SAFE_ID_RE`, `cli/lib/pull.py:83`); every `source_corpus_ids` entry must exist in `file_corpora` (400 otherwise); duplicate slug → 409\n- `GET /api/admin/knowledge-digests/{digest_id}` → full digest incl. `output_md`, `status`, `status_reason`\n- `PUT /api/admin/knowledge-digests/{digest_id}` → update `title`/`instructions`/`source_corpus_ids` (slug immutable — not in the model)\n- `DELETE /api/admin/knowledge-digests/{digest_id}` → 204; also `resource_grants_repo().delete_by_resource(...)` (`src/repositories/resource_grants.py:256`) so no orphan grants linger\n\nAll `Depends(require_admin)`. Audit actions `knowledge_digest.create/update/delete` — copy the `_audit` helper idiom of `app/api/memory_domains.py:96` (that module is the structural template for the whole file; it too \"mirrors data_packages\" per its docstring).\n\n- [ ] **Step 1: Failing tests** (`tests/test_api_knowledge_digests.py`) — mirror the `seeded_app` / `_auth` fixture idiom of `tests/test_api_collections.py`:\n  - 401 unauthenticated, 403 analyst on every method;\n  - admin POST → 201 with `status == \"pending\"`; slug `\"Bad Slug!\"` → 422/400; unknown corpus id in `source_corpus_ids` → 400; duplicate slug → 409;\n  - GET list/detail shapes; PUT edits instructions and resets nothing else (status untouched — regeneration is the PASS's job, triggered because the fingerprint covers instructions, see Task 4); DELETE → 204 then GET → 404.\n- [ ] **Step 2: Run** → FAIL (404 route)\n- [ ] **Step 3: Implement.** `app/resource_types.py`:\n\n```python\n    KNOWLEDGE_DIGEST = \"knowledge_digest\"      # enum member, app/resource_types.py:34ff\n\ndef _knowledge_digest_blocks() -> List[Block]:\n    \"\"\"Project knowledge_digests into the (block → items) tree for /admin/access.\n\n    Single block; one item per digest. ``resource_id`` is the digest id —\n    the exact string the manifest/content-endpoint RBAC checks against.\n    \"\"\"\n    from src.repositories import knowledge_digests_repo\n\n    items = [\n        {\"resource_id\": d[\"id\"], \"name\": d[\"title\"], \"slug\": d[\"slug\"], \"status\": d.get(\"status\")}\n        for d in knowledge_digests_repo().list()\n    ]\n    return [{\"id\": \"knowledge_digests\", \"name\": \"Maintained digests\", \"items\": items}]\n\n    ResourceType.KNOWLEDGE_DIGEST: ResourceTypeSpec(          # RESOURCE_TYPES entry\n        key=ResourceType.KNOWLEDGE_DIGEST,\n        display_name=\"Maintained digests\",\n        description=(\n            \"An admin-defined digest document regenerated from its source \"\n            \"collections. Grant a group access so `agnes pull` delivers it \"\n            \"to members as .claude/rules/ka_<slug>.md.\"\n        ),\n        id_format=\"<digest_id>\",\n        list_blocks=_knowledge_digest_blocks,\n    ),\n```\n\nNo DB migration (application-level registry — `app/resource_types.py:22-25`). The `/admin/access` page picks the new type up automatically.\n\n`app/api/knowledge_digests.py` — Pydantic models with field validators (slug regex, title ≤200, instructions 1–20000 chars, `source_corpus_ids` ≤50 entries), handlers thin over `knowledge_digests_repo()`; corpus-existence check via `{c[\"id\"] for c in file_corpora_repo().list()}`.\n\n- [ ] **Step 4: Classify + cover.** `_COHORT` additions (surfaces land in Task 7 — commit Tasks 3+7 branches together or accept the ratchet red between them; recommended: write the `_COHORT` entries in THIS task and let Task 7 make them green before the Task-3 branch merges — with subagent-driven development, run them as one review unit):\n\n```python\n    # Maintained digests (K4, #799) — admin CRUD, triple-surface.\n    \"/api/admin/knowledge-digests\": (\"admin digest list\", \"admin_knowledge_digests_list\"),\n    \"/api/admin/knowledge-digests/{digest_id}\": (\"admin digest show\", \"admin_knowledge_digest_get\"),\n```\n\n`tests/db_pg/test_endpoints_smoke.py`: COVERED_ROUTES entries + admin happy-path smokes for `GET/POST /api/admin/knowledge-digests` and `GET/PUT/DELETE /api/admin/knowledge-digests/{digest_id}` (follow the file's class conventions, `tests/db_pg/test_endpoints_smoke.py:3-6`). `docs/api-reference.md`: one line per endpoint (docs-coverage gate must be green at THIS commit).\n\n- [ ] **Step 5: Run** targeted tests + `tests/test_api_docs_coverage.py` → PASS\n- [ ] **Step 6: Commit** `feat(api,rbac): knowledge-digest CRUD + KNOWLEDGE_DIGEST resource type (K4)`\n\n---\n\n### Task 4: `src/knowledge_digests.py` — fingerprint short-circuit + LLM regeneration pass — *[parallel with Task 3 after Task 2]*\n\n**Files:**\n- Create: `src/knowledge_digests.py`\n- Modify: `config/instance.yaml.example` (documented `digests:` block next to the `ai:` block, `config/instance.yaml.example:~298`)\n- Test: `tests/test_knowledge_digests.py`\n\n**Interfaces:**\n\n```python\ndef digest_fingerprint(digest: dict) -> str\n    # md5 over: md5(instructions) | sorted corpus ids | per-corpus\n    # src.knowledge_packaging.corpus_fingerprint — flips on ANY source chunk\n    # change AND on instruction/source-list edits (an edited digest definition\n    # must regenerate without a separate trigger).\n\ndef run_digest_pass() -> dict\n    # {\"generated\": [slug…], \"skipped\": [slug…], \"stale\": [{\"slug\", \"reason\"}…],\n    #  \"errors\": [{\"slug\", \"error\"}…]}  — concurrency 1, sequential, per-row\n    # try/except (the _run_materialized_pass posture, app/api/sync.py:145).\n\n# seams (tests patch these; production uses the real things):\ndef _repo():                  from src.repositories import knowledge_digests_repo; return knowledge_digests_repo()\ndef _corpus_fingerprint(cid): from src.knowledge_packaging import corpus_fingerprint; return corpus_fingerprint(cid)\ndef _list_chunks(cid):        from src.knowledge_packaging import _list_chunks as f; return f(cid)\ndef _make_extractor():        # create_extractor_from_env_or_config(get_value(\"ai\") …) — collector.py:441 idiom\n```\n\n**Pass semantics (write them in the module docstring):**\n1. Build the extractor ONCE. If it raises `ValueError` (no `ai:` block, no env key — `services/corporate_memory/collector.py:429-448`), do NOT crash: every digest whose fingerprint changed is `mark_stale(reason=\"LLM not configured: …\")`, unchanged digests are skipped, summary carries one `errors` entry `{\"slug\": \"*\", \"error\": \"llm_unconfigured: …\"}`. Visible (status + scheduler-runs), never silent.\n2. Per digest: `fp = digest_fingerprint(d)`; if `fp == d[\"source_fingerprint\"] and d[\"status\"] == \"fresh\"` → skipped, **no LLM call** (the fingerprint short-circuit; `pending`/`stale` rows always retry).\n3. Budget gates BEFORE each generation: generations this pass ≥ `digests.llm.max_generations_per_pass` → `mark_stale(reason=\"deferred: per-pass generation budget exhausted\")`; wall clock past `digests.llm.timeout_seconds` → `mark_stale(reason=\"deferred: pass deadline exceeded\")`. Both retry next pass.\n4. Generate: prompt = standing `instructions` + previous `output_md` (if any) + source chunks concatenated per corpus, hard-capped at `_SOURCE_CHAR_BUDGET = 120_000` chars with an explicit `[…truncated]` marker; `extractor.extract_json(prompt, max_tokens=<knob>, json_schema={\"type\": \"object\", \"properties\": {\"markdown\": {\"type\": \"string\"}}, \"required\": [\"markdown\"]}, schema_name=\"knowledge_digest\")`. Model recorded via `getattr(extractor, \"_model\", None)`.\n5. Success → ONE `set_generated(...)` call with the fp computed BEFORE generation (a mid-generation ingest flips the fingerprint next pass — acceptable staleness window, same as K3's packaging). Empty/whitespace markdown from the model → treat as failure (`mark_stale(reason=\"LLM returned empty digest\")`), keep previous output.\n6. Any `LLMError`/exception → `mark_stale(reason=f\"{type(e).__name__}: {e}\"[:500])` + `errors` entry. One broken digest never stops siblings.\n\n- [ ] **Step 1: Failing tests** (`tests/test_knowledge_digests.py`) — real DuckDB repo over `_ensure_schema(tmp db)` wired through a patched `_repo` seam; fake extractor object with a `calls` list:\n\n```python\ndef test_unchanged_sources_no_llm_call(...):        # generate once, run again with same\n    # fingerprints → second pass: skipped == [slug], fake extractor called exactly once total\ndef test_changed_fingerprint_regenerates(...):      # flip _corpus_fingerprint return → generated\ndef test_instruction_edit_flips_fingerprint(...):   # digest_fingerprint(d1) != digest_fingerprint(d1|instructions edited)\ndef test_llm_failure_keeps_previous_output_marks_stale(...):\n    # extractor raises LLMTimeoutError → row: status 'stale', reason set,\n    # output_md STILL the previous markdown, source_fingerprint unchanged (retries next pass)\ndef test_no_api_key_marks_changed_digests_stale_not_crash(...):\n    # _make_extractor raises ValueError → run_digest_pass returns; changed digest stale\n    # with \"LLM not configured\", unchanged digest skipped and still 'fresh'\ndef test_generation_budget_defers_excess(...):      # 4 changed digests, max_generations_per_pass=2\n    # → 2 generated, 2 stale with \"deferred: per-pass generation budget\"\ndef test_empty_markdown_is_failure_not_wipe(...):   # extractor returns {\"markdown\": \"  \"} → stale, old output kept\ndef test_pending_digest_generates_first_time(...)   # no fingerprint yet → generated, status fresh\n```\n\n- [ ] **Step 2: Run** → FAIL (module missing)\n- [ ] **Step 3: Implement** per the semantics above (module docstring must state the naming decision, the knob drift note, and the never-half-written invariant). `config/instance.yaml.example` gains a commented `digests:` block documenting the three knobs + the fact that provider/key/model ride the existing `ai:` section.\n- [ ] **Step 4: Run** `.venv/bin/pytest tests/test_knowledge_digests.py -q` → PASS\n- [ ] **Step 5: Commit** `feat(knowledge): digest engine — fingerprint short-circuit + budgeted LLM pass (K4)`\n\n---\n\n### Task 5: `POST /api/admin/run-knowledge-digests` + scheduler job — *[after Task 4]*\n\n**Files:**\n- Modify: `app/api/admin.py` (immediately after `run_knowledge_packaging`, `app/api/admin.py:4194`)\n- Modify: `services/scheduler/__main__.py` (`_DEFAULTS` `services/scheduler/__main__.py:74`, `build_jobs()` `services/scheduler/__main__.py:296`)\n- Modify: `tests/test_documentation_api_triple_surface.py` (`_EXEMPT`), `tests/db_pg/test_endpoints_smoke.py`, `tests/test_scheduler_sidecar.py`, `docs/api-reference.md`\n- Test: extend `tests/test_api_knowledge_digests.py`\n\n- [ ] **Step 1: Failing tests** — (a) non-admin POST → 403; admin POST → 200 `{\"ok\": bool, \"details\": {generated, skipped, stale, errors}}` (patch `src.knowledge_digests.run_digest_pass` at the endpoint's import seam); (b) `tests/test_scheduler_sidecar.py`: `build_jobs()` contains `(\"knowledge-digests\", \"every 30m\", \"/api/admin/run-knowledge-digests\", \"POST\", 900)`, honors `SCHEDULER_KNOWLEDGE_DIGESTS_INTERVAL` override, and — **watch item** — `test_build_jobs_uses_documented_defaults` (`tests/test_scheduler_sidecar.py:6`) enumerates jobs: extend it, don't fight it.\n- [ ] **Step 2: Run** → FAIL\n- [ ] **Step 3: Implement.** Endpoint is a line-for-line sibling of `run_knowledge_packaging` (`app/api/admin.py:4194-4235`): call `run_digest_pass()`, audit `action=\"run_knowledge_digests\"`, `resource=\"job:knowledge-digests\"`, params = counts of generated/skipped/stale/errors (+ `unhandled_error` → 500 re-raise). Scheduler: `_DEFAULTS[\"SCHEDULER_KNOWLEDGE_DIGESTS_INTERVAL\"] = 30 * 60` (comment: fingerprint-only when idle — cheap; LLM only on change; corporate-memory cadence class), `_read_positive_int` in `build_jobs()`, include in the `smallest = min(...)` guard, append the job tuple with **900 s** timeout (LLM job — same class as `corporate-memory` at `services/scheduler/__main__.py:355`).\n- [ ] **Step 4: Classify + cover.** `_EXEMPT`:\n\n```python\n\"/api/admin/run-knowledge-digests\": (\n    \"scheduler-driven digest regeneration trigger (K4, #799) — admin/scheduler \"\n    \"maintenance op, mirrors the run-knowledge-packaging / run-corporate-memory \"\n    \"exemptions; no analyst CLI/MCP analogue\"\n),\n```\n\nSmoke COVERED_ROUTES entry `\"POST /api/admin/run-knowledge-digests\"` + `docs/api-reference.md` line.\n\n- [ ] **Step 5: Run** targeted + `tests/test_scheduler_sidecar.py` → PASS\n- [ ] **Step 6: Commit** `feat(admin,scheduler): knowledge-digests trigger + 30m schedule (K4)`\n\n---\n\n### Task 6: manifest `kind:\"digest\"` entries + `GET /api/knowledge/digests/{digest_id}/content` — *[parallel with Tasks 7–8 after Tasks 2+3]*\n\n**Files:**\n- Modify: `app/api/sync.py` (`_build_knowledge_artifacts_section`, `app/api/sync.py:1164`)\n- Modify: `app/api/knowledge_search.py` (new route on the existing `/api/knowledge` router, next to the artifact download at `app/api/knowledge_search.py:95`)\n- Modify: `tests/test_documentation_api_triple_surface.py` (`_EXEMPT`), `tests/db_pg/test_endpoints_smoke.py`, `docs/api-reference.md`\n- Test: `tests/test_api_knowledge_digests_distribution.py`\n\n**Frozen manifest contract (Task 9 codes against this):** digest entries are appended to the EXISTING `knowledge_artifacts` list:\n\n```python\n{\"kind\": \"digest\", \"id\": \"kd_…\", \"slug\": \"architecture-overview\",\n \"title\": \"Architecture overview\", \"status\": \"fresh\" | \"stale\",\n \"status_reason\": None | \"…\", \"generated_at\": \"…\",\n \"md5\": \"…\",                      # change-token, see below\n \"url\": \"/api/knowledge/digests/kd_…/content\"}\n```\n\n`md5 = hashlib.md5(f\"{slug}|{status}|{status_reason or ''}|{generated_at or ''}|{output_md or ''}\".encode()).hexdigest()` — it must flip when EITHER content OR staleness flips, so a digest going stale re-fetches and the banner reaches the laptop. Document honestly: unlike K3's artifact md5 this is a **change-detection token**, not a byte-level integrity check of the downloaded file (the JSON body over TLS+PAT is the truth — the memory-bundle posture, per-domain md5 precedent at `app/api/sync.py:1202`).\n\n- [ ] **Step 1: Failing tests** — mirror `tests/test_api_knowledge_artifacts.py`:\n  - seed a digest via the repo with `set_generated(...)`; `GET /api/sync/manifest` as admin → `knowledge_artifacts` contains the `kind==\"digest\"` entry with `md5`/`url`; K3 `kind==\"chunks\"` entries still co-exist untouched;\n  - ungranted analyst → digest entry absent (grant the analyst's group `knowledge_digest`/`kd_…` via `resource_grants_repo().create(...)` → present); a `pending` digest (no `output_md`) → never listed;\n  - stale digest → listed with `status == \"stale\"` + reason, and its `md5` differs from the fresh-state md5;\n  - content endpoint: 401 unauthenticated; 404 ungranted (no existence leak); 404 unknown id; 200 admin → `{\"id\", \"slug\", \"title\", \"output_md\", \"status\", \"status_reason\", \"generated_at\"}`.\n- [ ] **Step 2: Run** → FAIL\n- [ ] **Step 3: Implement.** In `app/api/sync.py`, restructure `_build_knowledge_artifacts_section` (it currently early-returns on empty packaging state, `app/api/sync.py:1178-1179` — digests must be listed even when no chunk artifact exists): `out = _chunk_artifact_entries(user) + _digest_entries(user)`. `_digest_entries` filters like `_accessible_corpus_ids` does (`app/api/collections.py:180-192`): `can_access(uid, ResourceType.KNOWLEDGE_DIGEST.value, d[\"id\"])` / `can_access_session(...)` for `SessionPrincipal`, skips rows with empty `output_md`, sorts by slug. Keep the section's own `try/except → []` wrapper at the call site (`app/api/sync.py:1403`) untouched.\n\nIn `app/api/knowledge_search.py`:\n\n```python\n@router.get(\"/digests/{digest_id}/content\")\nasync def get_knowledge_digest_content(digest_id: str, user=Depends(get_current_user)):\n    \"\"\"Serve one maintained digest's markdown (K4, #799).\n\n    Consumed by ``agnes pull`` (writes ``.claude/rules/ka_<slug>.md``); the\n    PAT is the only credential. RBAC = knowledge_digest resource grants,\n    fail-closed: ungranted, unknown, or never-generated → 404 (no\n    existence leak). Staleness (status + reason) travels in the body so the\n    client can render a visible banner — never a silent stale digest.\n    \"\"\"\n    from src.repositories import knowledge_digests_repo\n    # RBAC check mirrors download_knowledge_artifact (this file, :95) —\n    # can_access / can_access_session with ResourceType.KNOWLEDGE_DIGEST.\n    d = knowledge_digests_repo().get(digest_id)\n    if d is None or not (d.get(\"output_md\") or \"\").strip() or not _caller_can_read_digest(user, digest_id):\n        raise HTTPException(status_code=404, detail=\"Digest not found\")\n    # audit knowledge.digest_download (best-effort try/except, the :122-132 idiom)\n    return {\"id\": d[\"id\"], \"slug\": d[\"slug\"], \"title\": d[\"title\"],\n            \"output_md\": d[\"output_md\"], \"status\": d[\"status\"],\n            \"status_reason\": d.get(\"status_reason\"),\n            \"generated_at\": str(d.get(\"generated_at\") or \"\")}\n```\n\n(Order the checks so ungranted and unknown are indistinguishable. Share `_caller_can_read_digest` with the sync.py section builder — put it in `app/api/knowledge_search.py` and import lazily from sync.py, matching the existing lazy-import pattern for `_accessible_corpus_ids`.)\n\n- [ ] **Step 4: Classify + cover.** `_EXEMPT`:\n\n```python\n\"/api/knowledge/digests/{digest_id}/content\": (\n    \"K4 maintained digests (#799) — digest markdown consumed by `agnes pull` \"\n    \"(written to .claude/rules/ka_<slug>.md, pruned on de-authorization); \"\n    \"no interactive CLI/MCP analogue, mirrors the knowledge-artifact \"\n    \"download and /api/memory/bundle delivery channels\"\n),\n```\n\nSmoke COVERED_ROUTES + `docs/api-reference.md` line.\n\n- [ ] **Step 5: Run** targeted + `tests/test_api_knowledge_artifacts.py` (K3 regressions) → PASS\n- [ ] **Step 6: Commit** `feat(api,sync): digest manifest entries + content endpoint (K4)`\n\n---\n\n### Task 7: CLI `agnes admin digest` + MCP tools (closes the `_COHORT` entries) — *[parallel with Tasks 6, 8 after Task 3]*\n\n**Files:**\n- Create: `cli/commands/admin_digest.py`; Modify: `cli/commands/admin.py` (`admin_app.add_typer(admin_digest_app, name=\"digest\", help=\"Maintained digest CRUD (K4)\")` — the `admin_data_package_app` registration idiom at `cli/commands/admin.py:48`)\n- Modify: `app/api/mcp_http.py` (tools `admin_knowledge_digests_list`, `admin_knowledge_digest_get`, `admin_knowledge_digest_create`, `admin_knowledge_digest_update`, `admin_knowledge_digest_delete`)\n- Test: `tests/test_cli_admin_digest.py` + ratchet run\n\n- [ ] **Step 1: CLI** — template on `cli/commands/admin_data_package.py` (same `_fail` helper, `--yes` confirm on delete, `<digest>` accepts id or slug with slug→id resolution via list-and-match): subcommands `list`, `show`, `create` (`--slug --title --instructions/--instructions-file --source <corpus_id> (repeatable)`), `edit`, `delete`, each mapping 1:1 to one REST call via `cli.client.api_get/api_post/api_put/api_delete`. `show` prints status + reason prominently (`STALE — <reason>` in the output, staleness is a first-class display concern).\n- [ ] **Step 2: MCP** — in `app/api/mcp_http.py`, HTTP-loopback tools (the `stack_browse` idiom at `app/api/mcp_http.py:344` — `httpx.AsyncClient` + `_headers()`, `raise_for_status`, so `require_admin` is enforced by the REST layer). Docstrings must explain what a maintained digest is and that grants control who receives `ka_<slug>.md` on pull.\n- [ ] **Step 3: Tests** — Typer `CliRunner` tests mirroring the sibling admin CLI tests (list/show/create happy path against a mocked `api_get`/`api_post`; delete requires `--yes`). Then `.venv/bin/pytest tests/test_documentation_api_triple_surface.py -q` → the Task-3 `_COHORT` entries now PASS (CLI tree check walks `cli/main.py`'s registered groups, 3-level `admin digest list` is supported — `tests/test_documentation_api_triple_surface.py:101-120`; MCP check runs `mcp.list_tools()` against `app.api.mcp_http`).\n- [ ] **Step 4: Commit** `feat(cli,mcp): agnes admin digest + knowledge-digest MCP tools (K4)`\n\n---\n\n### Task 8: `/admin/knowledge-digests` UI page — *[parallel with Tasks 6, 7 after Task 3]*\n\n**Files:**\n- Modify: `app/web/router.py` (page handler next to `admin_mcp_sources_page`, `app/web/router.py:3144`)\n- Create: `app/web/templates/admin_knowledge_digests.html`\n- Modify: `app/web/templates/_app_header.html` (admin menu item + the `_admin_active` path list at `_app_header.html:51`)\n- Test: `tests/test_admin_knowledge_digests_page.py` (mirror `tests/test_admin_data_sources_page.py`)\n\n- [ ] **Step 1: Failing test** — admin GET `/admin/knowledge-digests` → 200 containing the page heading and the seeded digest's title + a visible `stale` badge when `status == \"stale\"`; analyst → 403/redirect per the sibling page's behavior; nav link present for admins.\n- [ ] **Step 2: Implement.** Handler: `require_admin`, `_build_context`, `TemplateResponse(\"admin_knowledge_digests.html\", ctx)` — server renders the shell only; data + CRUD go through `fetch()` against `/api/admin/knowledge-digests` (the `admin_mcp_sources.html` pattern). Template rules (CLAUDE.md:309, guarded by `tests/test_design_system_contract.py`): `{% extends \"base_ds.html\" %}`, body in `{% block content %}`, page CSS in `{% block head_extra %}`, `ds.*` macros auto-imported, **never** `base.html`, no raw `#hex`, no `var(--primary)` (use `var(--ds-primary)`), no bare `:root{}`. Page contents: table (slug, title, sources count, status badge — `fresh` green / `stale` amber with `status_reason` tooltip / `pending` neutral —, `generated_at`, model), create/edit dialog (title, slug [create only], instructions textarea, source-collections multi-select fed from `GET /api/collections`), delete with confirm, and a \"grants are managed on the Resource access page\" pointer linking `/admin/access`. Follow the step-by-step recipe in `docs/architecture.md` → *Extending the Platform → New Web Page*.\n- [ ] **Step 3: Run** page test + `tests/test_design_system_contract.py` → PASS\n- [ ] **Step 4: Commit** `feat(web): /admin/knowledge-digests page (K4)`\n\n---\n\n### Task 9: `agnes pull` — deliver `.claude/rules/ka_<slug>.md` — *[independent; parallel with Tasks 1–8; codes against Task 6's frozen contract]*\n\n**Files:**\n- Modify: `cli/lib/pull.py` (`PullResult` fields, `_sync_knowledge_digests`, call site, one-line hardening of the K3 loop)\n- Test: `tests/test_lib_pull_digests.py` (the `tests/test_lib_pull_knowledge.py` stub-server idiom: monkeypatch `cli.lib.pull.api_get`, autouse `AGNES_CONFIG_DIR` sandbox)\n\n**Interfaces:**\n- `PullResult` gains `digests_updated: int = 0`, `digests_removed: int = 0` (documented in the dataclass docstring, `cli/lib/pull.py:45-80`).\n- Sync-state gains a `knowledge_digests` dict (`{digest_id: {\"md5\": …, \"slug\": …}}`) in the same persisted payload — the new step MUST run before step-5 `save_sync_state` (`cli/lib/pull.py:726`).\n- The `ka_*.md` namespace in `.claude/rules/` is **server-managed** exactly like `km_*.md` (`cli/lib/pull.py:1000-1003`): `_sync_knowledge_digests` is the only writer and prunes stale `ka_*.md` files; it never touches `km_*.md`.\n\n- [ ] **Step 1: Failing tests** (`tests/test_lib_pull_digests.py`) — manifest fixtures use the frozen Task-6 entry shape:\n\n```python\ndef test_writes_ka_file_with_title_and_content(...):\n    # manifest knowledge_artifacts = [digest entry, status fresh] →\n    # .claude/rules/ka_architecture-overview.md exists, contains title h1 +\n    # output_md, NO stale banner; result.digests_updated == 1\ndef test_stale_digest_gets_visible_banner(...):\n    # status \"stale\" + status_reason → file contains \"STALE\" and the reason\n    # (the never-silent invariant reaches the laptop)\ndef test_unchanged_md5_skips_fetch(...):\n    # second run, same md5 → api_get NOT called for the content url\ndef test_staleness_flip_refetches(...):\n    # same output, md5 changed because status flipped → re-fetched, banner appears\ndef test_prunes_on_deauthorization(...):\n    # pre-existing ka_gone.md; key PRESENT with [] → pruned, digests_removed == 1\ndef test_pre_k4_server_key_missing_leaves_tree_untouched(...):\n    # manifest without knowledge_artifacts key → ka_keep.md survives\ndef test_chunks_entries_ignored_by_digest_step_and_vice_versa(...):\n    # mixed section: kind chunks + kind digest → duckdb artifact loop untouched\n    # by digest entry, ka_ writer ignores chunks entry\ndef test_unsafe_slug_skipped(...):\n    # slug \"../evil\" fails _SAFE_ID_RE → no file written, no crash\ndef test_fetch_error_keeps_prior_file(...):\n    # api_get raises for the content url → old ka_*.md intact, stage error recorded\n```\n\n- [ ] **Step 2: Run** → FAIL (`PullResult` has no `digests_updated`)\n- [ ] **Step 3: Implement.**\n  - **Hardening (one line):** in `_sync_knowledge_artifacts` (`cli/lib/pull.py:869`), before the `corpus_id` read: `if entry.get(\"kind\") not in (None, \"chunks\"): continue` — today digest entries are skipped only *accidentally* (empty `corpus_id` fails `_SAFE_ID_RE` at `cli/lib/pull.py:872`); make it explicit.\n  - New helper (place near `_fetch_and_write_rules`, `cli/lib/pull.py:989`):\n\n```python\ndef _sync_knowledge_digests(manifest: dict, workspace: Path, local_state: dict, result: \"PullResult\") -> None:\n    \"\"\"K4 (#799): write/prune maintained digests as ``.claude/rules/ka_<slug>.md``.\n\n    Same delivery as the corporate-memory ``km_*.md`` bundle — the digest is\n    in the agent's context at session start. Gate: the ``knowledge_artifacts``\n    KEY must be present (pre-K3/K4 servers must not nuke the local tree); a\n    present list with zero ``kind==\"digest\"`` entries prunes all ``ka_*.md``\n    (de-authorization / deletion). The manifest ``md5`` is a change-token\n    covering content AND staleness, so a digest going stale re-fetches and\n    the banner below lands on the laptop — staleness is never silent.\n    \"\"\"\n    section = manifest.get(\"knowledge_artifacts\")\n    if section is None:\n        return\n    entries = [e for e in section or [] if e.get(\"kind\") == \"digest\"]\n    rules_dir = Path(workspace) / \".claude\" / \"rules\"\n    known = local_state.setdefault(\"knowledge_digests\", {})\n    listed_files: set[str] = set()\n    for entry in entries:\n        slug, did, md5 = entry.get(\"slug\") or \"\", entry.get(\"id\") or \"\", entry.get(\"md5\") or \"\"\n        if not _SAFE_ID_RE.match(slug) or not _SAFE_ID_RE.match(did):\n            continue\n        fname = f\"ka_{slug}.md\"\n        listed_files.add(fname)\n        target = rules_dir / fname\n        if md5 and known.get(did, {}).get(\"md5\") == md5 and target.exists():\n            continue\n        try:\n            resp = api_get(entry.get(\"url\") or f\"/api/knowledge/digests/{did}/content\")\n            resp.raise_for_status()\n            body = resp.json()\n            rules_dir.mkdir(parents=True, exist_ok=True)   # lazy mkdir, km_ contract\n            target.write_text(_digest_to_md(body), encoding=\"utf-8\")\n            known[did] = {\"md5\": md5, \"slug\": slug}\n            result.digests_updated += 1\n        except Exception as exc:\n            result.errors.append({\"stage\": \"knowledge_digests\", \"digest\": slug, \"error\": str(exc)})\n    if rules_dir.exists():\n        for f in sorted(rules_dir.glob(\"ka_*.md\")):\n            if f.name in listed_files:\n                continue\n            f.unlink(missing_ok=True)\n            result.digests_removed += 1\n        known_ids = {d for d, meta in known.items() if f\"ka_{meta.get('slug')}.md\" in listed_files}\n        for gone in set(known) - known_ids:\n            known.pop(gone, None)\n\n\ndef _digest_to_md(body: dict) -> str:\n    lines = [f\"# {body.get('title') or body.get('slug')}\", \"\"]\n    lines.append(\n        f\"_Maintained digest `ka_{body.get('slug')}` — regenerated by Agnes when \"\n        f\"its sources change (last generated: {body.get('generated_at') or 'never'}). \"\n        \"Server-managed; do not edit._\"\n    )\n    if body.get(\"status\") == \"stale\":\n        lines += [\"\", f\"> ⚠ **STALE** — {body.get('status_reason') or 'regeneration failed'}. \"\n                      \"Content below is the last successful generation.\"]\n    lines += [\"\", body.get(\"output_md\") or \"\"]\n    return \"\\n\".join(lines)\n```\n\n  - Call site: step **4d**, immediately after the K3 `_sync_knowledge_artifacts` block (`cli/lib/pull.py:710-716`) and before step-5 `save_sync_state`, wrapped best-effort with `{\"stage\": \"knowledge_digests\", \"error\": …}` — the same posture as 4c. (`dry_run` already returned at step 3 — digests untouched on dry runs.)\n- [ ] **Step 4: Run** `tests/test_lib_pull_digests.py tests/test_lib_pull_knowledge.py tests/test_lib_pull.py tests/test_pull_invariants.py -q` → PASS (K3 pull tests are the regression guard for the kind-filter hardening)\n- [ ] **Step 5: Commit** `feat(cli): agnes pull delivers maintained digests as .claude/rules/ka_<slug>.md (K4)`\n\n---\n\n### Task 10: docs, CHANGELOG, full suite\n\n- [ ] `docs/api-reference.md` — confirm all five surfaces landed their lines (Tasks 3/5/6); `.venv/bin/pytest tests/test_api_docs_coverage.py -q` green.\n- [ ] CHANGELOG `### Added` (under `[Unreleased]`):\n\n```markdown\n- Maintained digests: admins define digest documents (title + standing\n  instructions + source collections) that the scheduler regenerates with an\n  LLM only when the sources' content fingerprint changes. Failures keep the\n  previous markdown and mark the digest visibly stale (status + reason) —\n  never silent, never half-written. Digests are granted via standard resource\n  grants, listed in the sync manifest, and `agnes pull` delivers them as\n  `.claude/rules/ka_<slug>.md` (staleness banner included) so they are in the\n  agent's context at session start. Admin CRUD via REST +\n  `/admin/knowledge-digests` + `agnes admin digest` + MCP (#799).\n```\n\n- [ ] Full suite `.venv/bin/pytest tests/ --tb=short -n auto -q` → PASS\n- [ ] Commit `docs: changelog for maintained digests`\n\n---\n\n## Execution notes\n\n- Branch `zs/k4-maintained-digests` **off `origin/main` after PR #823 (K3) merges**. First action on the fresh branch: re-verify `SCHEMA_VERSION` (`src/db.py:50`), the ladder tail (`_v87_to_v88` today), and the Alembic head (`0035_parent_file_id_v88` today) — renumber Task 1 if main moved (K1 precedent). Also re-confirm K3's seams survived review unchanged: `src/knowledge_packaging.corpus_fingerprint`, `_build_knowledge_artifacts_section` (`app/api/sync.py:1164`), `_sync_knowledge_artifacts` (`cli/lib/pull.py:847`).\n- Task order: 1 → 2 → {3, 4} → {5 (after 4), 6, 7, 8 (after 3)} → 10. Task 9 can start immediately in parallel — its stub-server tests code against the Task-6 manifest contract, which is frozen by this plan.\n- Watch items: (a) `test_build_jobs_uses_documented_defaults` enumerates scheduler jobs — extend it; (b) the `_COHORT` entries (Task 3) are red until Task 7 lands — sequence the two tasks inside one review unit; (c) `app.api.sync` → `app.api.knowledge_search` imports must stay lazy in-function (the existing `_accessible_corpus_ids` idiom) to avoid cycles; (d) verify `create_extractor_from_env_or_config`'s exact name/signature in `connectors/llm/__init__.py` before wiring `_make_extractor` (the collector imports it at `services/corporate_memory/collector.py:441`); (e) `extract_json` accepts an optional `system=` kwarg on the Anthropic provider only — keep the digest prompt single-argument for provider portability, or verify `openai_compat` first.\n- After implementation: `/agnes-review` → fix → **release-cut check at PR time** (if this PR carries the only `[Unreleased]` content — likely, K3 cut 0.74.50 — the release-cut commit rides the same merge, per the sync-map's last row) → PR (record the three flagged decisions in the description: `knowledge_digests` table rename vs. spec, `status_reason` column addition, honest budget knobs vs. the unimplemented `ingest.llm.*` family, dedicated `KNOWLEDGE_DIGEST` resource type) → auto-merge watch.\n\n---\n\n### Critical Files for Implementation\n\n- /Users/zdeneksrotyr/Sources/VsCode/component_factory/tmp_oss/.claude/worktrees/laughing-cohen-4263e6/src/db.py (SCHEMA_VERSION:50, `_v87_to_v88`:5522, dispatch sites ~5890/~6119)\n- /Users/zdeneksrotyr/Sources/VsCode/component_factory/tmp_oss/.claude/worktrees/laughing-cohen-4263e6/src/knowledge_packaging.py (`corpus_fingerprint` reused by the digest fingerprint)\n- /Users/zdeneksrotyr/Sources/VsCode/component_factory/tmp_oss/.claude/worktrees/laughing-cohen-4263e6/app/api/sync.py (`_build_knowledge_artifacts_section`:1164, `_run_materialized_pass`:145 posture)\n- /Users/zdeneksrotyr/Sources/VsCode/component_factory/tmp_oss/.claude/worktrees/laughing-cohen-4263e6/cli/lib/pull.py (`_sync_knowledge_artifacts`:847, `_fetch_and_write_rules`:989, `PullResult`:45)\n- /Users/zdeneksrotyr/Sources/VsCode/component_factory/tmp_oss/.claude/worktrees/laughing-cohen-4263e6/app/resource_types.py (ResourceTypeSpec registry, no-migration grant wiring)"}],"stop_reason":"end_turn","stop_sequence":null,"stop_details":null,"usage":{"input_tokens":2,"cache_creation_input_tokens":5456,"cache_read_input_tokens":170117,"output_tokens":25500,"server_tool_use":{"web_search_requests":0,"web_fetch_requests":0},"service_tier":"standard","cache_creation":{"ephemeral_1h_input_tokens":0,"ephemeral_5m_input_tokens":5456},"inference_geo":"not_available","iterations":[{"input_tokens":2,"output_tokens":25500,"cache_read_input_tokens":170117,"cache_creation_input_tokens":5456,"cache_creation":{"ephemeral_5m_input_tokens":5456,"ephemeral_1h_input_tokens":0},"type":"message"}],"speed":"standard"},"diagnostics":null},"requestId":"req_011CcuAUpCMJgh3JptV1fiAk","attributionAgent":"Plan","type":"assistant","uuid":"4d8cf8e2-550e-46af-b4d3-3008dcf04159","timestamp":"2026-07-10T22:32:03.844Z","userType":"external","entrypoint":"claude-desktop","cwd":"/Users/zdeneksrotyr/Sources/VsCode/component_factory/tmp_oss/.claude/worktrees/laughing-cohen-4263e6","sessionId":"c6425535-f7bf-4f69-b835-2cba574dc5c8","version":"2.1.205","gitBranch":"zs/k3-local-knowledge-packaging"}
